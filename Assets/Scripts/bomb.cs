using UnityEngine;

public class bomb : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("I'm about to explode.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
